# AGENTS.md

이 저장소에서 AI 에이전트와 사람이 함께 작업할 때 지킬 규칙입니다.

## 기본 원칙

- 프로젝트 주제·기술 스택·하드웨어를 확정 전에 임의로 확정하지 않는다.
- 이미 동작하는 코드와 문서를 보존하고 요청 범위 밖의 변경을 피한다.
- 실제 검증 결과와 계획·예상을 명확히 구분한다.
- 토큰, 비밀번호, API 키, 개인정보, 비공개 데이터는 커밋하지 않는다.
- 큰 데이터셋, 모델 가중치, 빌드 결과는 Git에 직접 넣지 않고 출처와 받는 방법을 문서화한다.
- 외부 코드·모델·데이터의 출처와 라이선스를 기록한다.

## 작업 흐름

1. 작업 전 관련 Issue와 완료 조건을 확인한다.
2. 한 작업은 가능한 한 한 브랜치와 한 PR로 제한한다.
3. 변경 전 기존 구조와 테스트 방법을 읽는다.
4. 구현 후 관련 테스트와 문서 점검을 수행한다.
5. PR에는 변경 이유, 검증 결과, 미검증 범위, 위험 요소를 적는다.

## 구현 규칙

- 자체 구현과 외부 라이브러리·서비스의 경계를 문서와 설계도에 표시한다.
- 오류·취소·입력 누락 시 조용히 성공 처리하지 않는다.
- 재현 가능한 설치·실행·테스트 명령을 README에 유지한다.
- 새 기능에는 정상 경로와 대표 오류 경로 검증을 추가한다.
- 하드웨어가 필요한 검증은 실제 수행 여부를 별도로 표시한다.

## 문서 규칙

- 문제 정의 → 유스케이스 → HLD → 구현 → 검증이 서로 추적되게 한다.
- 수치나 성능 주장은 측정 조건과 증거를 함께 남긴다.
- `TODO`, 계획, 데모 대체물은 실제 완료 항목처럼 표현하지 않는다.
